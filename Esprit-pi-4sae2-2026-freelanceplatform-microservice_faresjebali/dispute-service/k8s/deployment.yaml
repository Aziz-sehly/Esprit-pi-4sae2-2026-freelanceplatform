apiVersion: apps/v1
kind: Deployment
metadata:
  name: dispute-service
  namespace: prolance
  labels:
    app: dispute-service
    tier: backend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: dispute-service
  template:
    metadata:
      labels:
        app: dispute-service
        tier: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path:   "/actuator/prometheus"
        prometheus.io/port:   "8087"
    spec:
      containers:
        - name: dispute-service
          image: prolance/dispute-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8087
          envFrom:
            - configMapRef:
                name: dispute-service-config
            - secretRef:
                name: dispute-service-secret
          resources:
            requests: { cpu: "150m", memory: "384Mi" }
            limits:   { cpu: "750m", memory: "768Mi" }
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8087 }
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8087 }
            initialDelaySeconds: 60
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: dispute-service
  namespace: prolance
  labels:
    app: dispute-service
spec:
  type: ClusterIP
  selector:
    app: dispute-service
  ports:
    - name: http
      port: 8087
      targetPort: 8087
