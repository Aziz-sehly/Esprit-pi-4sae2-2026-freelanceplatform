package com.prolance.user.domain;

/**
 * Rôles métier (pidev). {@link #USER} assure la compatibilité avec les lignes déjà présentes en base ProLance.
 */
public enum Role {
    USER,
    FREELANCER,
    CLIENT,
    ADMIN
}
